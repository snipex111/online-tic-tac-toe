<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tictactoe">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/FullscreenTheme">
        <activity android:name=".OnlineGameActivity"></activity>
        <activity android:name=".GameActivity" />
        <activity android:name=".blog.EditblogtextActivity" />
        <activity android:name=".blog.BlogActivity" />
        <activity android:name=".PlaywithcomputerActivity" />
        <activity android:name=".PlayActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".starter.LoginActivity" />
        <activity android:name=".starter.SignupActivity" />
        <activity android:name=".StartActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>